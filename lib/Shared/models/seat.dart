import 'dart:math';

class Seat {
  final bool reserved;
  bool selected = false;
  final int id;

  Seat({required this.reserved, required this.id});

  /// creates a random state for each seat from 25 available seats
 }
